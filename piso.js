piso.onsubmit=e=>{e.preventDefault();let area=+comp.value*+larg.value,fator=1+(+perda.value/100),areaF=area*fator,areaP=(+pecaC.value/100)*(+pecaL.value/100),pecas=Math.ceil(areaF/areaP),caixas=Math.ceil(areaF/+caixa.value),pc=+pecaC.value*10,pl=+pecaL.value*10,rej=((pc+pl)/(pc*pl))*(+junta.value)*(+esp.value)*1.6*areaF;prResultado.innerHTML=`<p>Área do ambiente<strong>${area.toFixed(2).replace('.',',')} m²</strong></p><p>Área com perda<strong>${areaF.toFixed(2).replace('.',',')} m²</strong></p><p>Peças estimadas<strong>${pecas} peças</strong></p><p>Caixas de piso<strong>${caixas} caixas</strong></p><p>Rejunte aproximado<strong>${rej.toFixed(2).replace('.',',')} kg</strong></p>`};document.querySelectorAll('.ano').forEach(x=>x.textContent=new Date().getFullYear());
