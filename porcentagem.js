const moeda=n=>new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(n);const r=document.getElementById('pResultado');document.querySelectorAll('.aba').forEach(b=>b.onclick=()=>{document.querySelectorAll('.aba').forEach(x=>x.classList.remove('ativa'));b.classList.add('ativa');document.querySelectorAll('.painel').forEach(x=>x.hidden=x.id!==b.dataset.painel);r.textContent='Preencha os dados e calcule.'});document.getElementById('p1').onsubmit=e=>{e.preventDefault();let x=+perc.value,y=+base.value;r.innerHTML=`<p><strong>${x}% de ${moeda(y)}</strong> é <strong>${moeda(y*x/100)}</strong>.</p>`};document.getElementById('p2').onsubmit=e=>{e.preventDefault();let x=+valor2.value,y=+perc2.value,z=operacao.value==='desconto'?x*(1-y/100):x*(1+y/100);r.innerHTML=`<p>O valor de <strong>${moeda(x)}</strong> com ${operacao.value} de ${y}% fica <strong>${moeda(z)}</strong>.</p>`};document.getElementById('p3').onsubmit=e=>{e.preventDefault();let x=+inicial.value,y=+final.value,z=(y-x)/x*100;r.innerHTML=`<p>A variação foi de <strong>${z.toFixed(2).replace('.',',')}%</strong> (${z>=0?'aumento':'redução'}).</p>`};document.querySelectorAll('.ano').forEach(x=>x.textContent=new Date().getFullYear());
